---
title: Workspace
---

export const PREVIEW_CODE = `import { LayoutDashboard, FileText, Settings } from "lucide-react"
import { Workspace } from "@/components/ui/workspace"

export function MyWorkspace() {
  return (
    <div className="h-[400px]">
      <Workspace
        initialPanes={[
          {
            id: "pane-a",
            tabs: [
              { id: "dashboard", title: "Dashboard", icon: <LayoutDashboard />, pinned: true },
              { id: "docs",      title: "Documentation", icon: <FileText /> },
            ],
            defaultSize: 60,
            minSize: 30,
          },
          {
            id: "pane-b",
            tabs: [{ id: "settings", title: "Settings", icon: <Settings />, pinned: true }],
            defaultSize: 40,
            minSize: 30,
          },
        ]}
        renderTabContent={(_paneId, tabId) => (
          <div className="flex h-full items-center justify-center">
            <p className="text-sm text-muted-foreground">{tabId}</p>
          </div>
        )}
        fallback={
          <div className="flex h-full items-center justify-center text-sm text-muted-foreground">
            All panels closed
          </div>
        }
      />
    </div>
  )
}`

export const WORKSPACE_TREE = {
  name: "Workspace",
  children: [
    {
      name: "WorkspacePaneDef (×n via initialPanes prop)",
      children: [
        {
          name: "WorkspaceTabDef (×n via tabs prop)",
          children: [
            { name: "icon?" },
            { name: "badge?" },
            { name: "pinned?" },
          ],
        },
      ],
    },
    { name: "renderTabContent(paneId, tabId) → your content" },
    { name: "fallback (shown when all panes closed)" },
  ],
}

Self-contained multi-pane workspace with tab drag/drop, cross-pane transfers, and snap-zone splitting. Exposes a `useWorkspace` context hook and a `WorkspaceHandle` ref for programmatic control.

<ComponentPreview name="workspace" code={PREVIEW_CODE} />

## Installation

<Tabs items={["CLI", "Manual"]}>
<Tab value="CLI">

```npm
npx shadcn@latest add jayclydeTags/workspaceui/workspace
```

</Tab>
<Tab value="Manual">

<Steps>
<Step>
### Install dependencies

```bash
npm install @base-ui/react react-resizable-panels lucide-react
```
</Step>
<Step>
### Copy the workspace files into your project

Workspace bundles an internal tab strip and drag context — copy all three files:

<ComponentSource name="workspace" />
<ComponentSource name="workspace-tabs" />
<ComponentSource name="workspace-context" />
</Step>
</Steps>

</Tab>
</Tabs>

## Usage

```tsx
import {
  Workspace,
  useWorkspace,
  type WorkspaceHandle,
} from "@/components/ui/workspace"
```

```tsx
<Workspace
  initialPanes={[
    {
      id: "editor",
      tabs: [{ id: "home", title: "Home", pinned: true }],
    },
  ]}
  renderTabContent={(_paneId, tabId) => (
    <div className="p-4">{tabId} content</div>
  )}
/>
```

### Opening tabs at runtime

`renderTabContent` is called on every render for the active tab of each pane, so
content lives in a lookup keyed by tab id — not inside the tabs. To open tabs
programmatically (e.g. from a sidebar), attach a `WorkspaceHandle` ref and call
`openTabInPane`. Reopening an already-open tab just focuses it.

```tsx
import * as React from "react"
import { LayoutDashboard, FileText, Settings } from "lucide-react"
import { Workspace, type WorkspaceHandle } from "@/components/ui/workspace"

// Content is looked up by tab id, defined once outside the component.
const TAB_CONTENT: Record<string, React.ReactNode> = {
  dashboard: <DashboardView />,
  documents: <DocumentsView />,
  settings: <SettingsView />,
}

export function App() {
  const workspace = React.useRef<WorkspaceHandle>(null)

  function openTab(id: string, title: string, icon: React.ReactNode) {
    // "main" is the target pane id from initialPanes; the tab is created if missing,
    // or focused if it already exists.
    workspace.current?.openTabInPane("main", { id, title, icon })
  }

  return (
    <div className="flex h-full">
      <nav className="w-48 shrink-0 border-r p-2">
        <button onClick={() => openTab("documents", "Documents", <FileText />)}>
          Documents
        </button>
        <button onClick={() => openTab("settings", "Settings", <Settings />)}>
          Settings
        </button>
      </nav>

      <Workspace
        ref={workspace}
        className="flex-1"
        initialPanes={[
          {
            id: "main",
            tabs: [
              { id: "dashboard", title: "Dashboard", icon: <LayoutDashboard />, pinned: true },
            ],
          },
        ]}
        renderTabContent={(_paneId, tabId) => TAB_CONTENT[tabId] ?? <div className="p-4">{tabId}</div>}
      />
    </div>
  )
}
```

Components rendered _inside_ a pane can reach the same methods without a ref via
the [`useWorkspace()`](#useworkspace) hook — use the ref from outside `<Workspace>`,
the hook from within it. Both expose `openTabInPane`, `closeTab`, `activateTab`,
`updateTab` (e.g. to set a tab's `badge`), `openPane`, and `closePane`.

## Examples

### Single pane, multiple tabs

One pane with several tabs. Tabs are closable and reorderable by drag; the pinned tab has no close button.

<ComponentPreview name="workspace-tabs" code={`import { Home, FolderOpen, Terminal } from "lucide-react"
import { Workspace } from "@/components/ui/workspace"

const CONTENT = { home: <Home />, files: <FolderOpen />, terminal: <Terminal /> }

export function MyWorkspace() {
  return (
    <Workspace
      initialPanes={[
        {
          id: "main",
          tabs: [
            { id: "home", title: "Home", icon: <Home />, pinned: true },
            { id: "files", title: "Files", icon: <FolderOpen /> },
            { id: "terminal", title: "Terminal", icon: <Terminal /> },
          ],
        },
      ]}
      renderTabContent={(_paneId, tabId) => CONTENT[tabId]}
    />
  )
}`} />

### Split panes

Two side-by-side panes sized with \`defaultSize\`. Drag a tab across the divider to move it between panes, or drag it to an edge to split further.

<ComponentPreview name="workspace-split" code={`import { LayoutDashboard, FileText, Settings } from "lucide-react"
import { Workspace } from "@/components/ui/workspace"

export function MyWorkspace() {
  return (
    <Workspace
      initialPanes={[
        {
          id: "left",
          defaultSize: 60,
          tabs: [
            { id: "dashboard", title: "Dashboard", icon: <LayoutDashboard />, pinned: true },
            { id: "docs", title: "Documentation", icon: <FileText /> },
          ],
        },
        {
          id: "right",
          defaultSize: 40,
          tabs: [{ id: "settings", title: "Settings", icon: <Settings />, pinned: true }],
        },
      ]}
      renderTabContent={(_paneId, tabId) => <div className="p-4">{tabId}</div>}
    />
  )
}`} />

### Tab badges

Set \`badge\` (a number) on a tab to show a count next to its title.

<ComponentPreview name="workspace-badges" code={`import { Inbox, MessageSquare, Bell } from "lucide-react"
import { Workspace } from "@/components/ui/workspace"

export function MyWorkspace() {
  return (
    <Workspace
      initialPanes={[
        {
          id: "main",
          tabs: [
            { id: "inbox", title: "Inbox", icon: <Inbox />, badge: 12, pinned: true },
            { id: "chat", title: "Chat", icon: <MessageSquare />, badge: 3 },
            { id: "alerts", title: "Alerts", icon: <Bell /> },
          ],
        },
      ]}
      renderTabContent={(_paneId, tabId) => <div className="p-4">{tabId}</div>}
    />
  )
}`} />

### Add-tab button

Provide \`onAddTab\` on a pane to render a "+" button in its tab strip. Open the new tab through the \`WorkspaceHandle\` ref.

<ComponentPreview name="workspace-add-tab" code={`import * as React from "react"
import { Home, FileText } from "lucide-react"
import { Workspace, type WorkspaceHandle } from "@/components/ui/workspace"

export function MyWorkspace() {
  const workspace = React.useRef<WorkspaceHandle>(null)
  const nextId = React.useRef(1)

  return (
    <Workspace
      ref={workspace}
      initialPanes={[
        {
          id: "main",
          tabs: [{ id: "start", title: "Start", icon: <Home />, pinned: true }],
          onAddTab: () => {
            const n = nextId.current++
            workspace.current?.openTabInPane("main", {
              id: \`tab-\${n}\`,
              title: \`Untitled \${n}\`,
              icon: <FileText />,
            })
          },
        },
      ]}
      renderTabContent={(_paneId, tabId) => <div className="p-4">{tabId}</div>}
    />
  )
}`} />

### Programmatic control (sidebar)

Drive the workspace from outside with a \`WorkspaceHandle\` ref. Each sidebar button calls \`openTabInPane\` — reopening an existing tab just focuses it.

<ComponentPreview name="workspace-programmatic" code={`import * as React from "react"
import { LayoutDashboard, FileText, Settings } from "lucide-react"
import { Workspace, type WorkspaceHandle } from "@/components/ui/workspace"

export function MyWorkspace() {
  const workspace = React.useRef<WorkspaceHandle>(null)

  const open = (id: string, title: string, icon: React.ReactNode) =>
    workspace.current?.openTabInPane("main", { id, title, icon })

  return (
    <div className="flex h-full">
      <nav className="w-44 shrink-0 border-r p-2">
        <button onClick={() => open("documents", "Documents", <FileText />)}>Documents</button>
        <button onClick={() => open("settings", "Settings", <Settings />)}>Settings</button>
      </nav>
      <Workspace
        ref={workspace}
        className="flex-1"
        initialPanes={[
          {
            id: "main",
            tabs: [{ id: "dashboard", title: "Dashboard", icon: <LayoutDashboard />, pinned: true }],
          },
        ]}
        renderTabContent={(_paneId, tabId) => <div className="p-4">{tabId}</div>}
      />
    </div>
  )
}`} />

### Control from inside a pane

Components rendered _inside_ a pane reach the same methods via the [\`useWorkspace()\`](#useworkspace) hook — no ref required.

<ComponentPreview name="workspace-context" code={`import { LayoutDashboard, FileText } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Workspace, useWorkspace } from "@/components/ui/workspace"

function OverviewPanel() {
  const { openTabInPane } = useWorkspace()
  return (
    <Button
      onClick={() =>
        openTabInPane("main", { id: "details", title: "Details", icon: <FileText /> })
      }
    >
      Open details tab
    </Button>
  )
}

export function MyWorkspace() {
  return (
    <Workspace
      initialPanes={[
        {
          id: "main",
          tabs: [{ id: "overview", title: "Overview", icon: <LayoutDashboard />, pinned: true }],
        },
      ]}
      renderTabContent={(_paneId, tabId) =>
        tabId === "overview" ? <OverviewPanel /> : <div className="p-4">{tabId}</div>
      }
    />
  )
}`} />

### Custom fallback

The \`fallback\` prop configures what shows when every pane is closed. Close the tab below to reveal it.

<ComponentPreview name="workspace-fallback" code={`import { FileText, Inbox } from "lucide-react"
import { Workspace } from "@/components/ui/workspace"

export function MyWorkspace() {
  return (
    <Workspace
      initialPanes={[
        { id: "main", tabs: [{ id: "scratch", title: "Scratch", icon: <FileText /> }] },
      ]}
      renderTabContent={(_paneId, tabId) => <div className="p-4">{tabId}</div>}
      fallback={
        <div className="flex h-full flex-col items-center justify-center gap-2">
          <Inbox className="size-8 text-muted-foreground/40" />
          <p className="text-sm text-muted-foreground">No panels open</p>
        </div>
      }
    />
  )
}`} />

## Composition

<ComponentTree description="Use the following composition to build a workspace:" root={WORKSPACE_TREE} />

## Features

- **Cross-pane drag and drop** — drag a tab from one pane and drop it into another to transfer it.
- **Snap-zone splitting** — drag a tab to an edge to split: left/right adds a new column, top/bottom adds a new row within the column.
- **Escape to cancel** — pressing Escape during a drag cancels the operation without committing a drop.
- **Resizable panels** — panel columns are resizable via drag handles, with configurable min sizes.
- **Unsaved-change guard** — mark a tab `dirty` to swap its close button for a dot, and return `false` from `onBeforeCloseTab` to veto the close.
- **Layout persistence** — `serialize()` / `restore()` on the `WorkspaceHandle` round-trip the column, pane, and tab structure through storage.
- **Imperative handle** — attach a `WorkspaceHandle` ref to control panes and tabs programmatically from outside the component.
- **Context hook** — any component rendered inside `<Workspace>` can call `useWorkspace()` to read state or open/close panes and tabs.
- **Custom fallback** — configure the view shown when all panes are closed via the `fallback` prop.

## API Reference

### Workspace

<TypeTable
  type={{
    initialPanes: { type: "WorkspacePaneDef[]", default: "[]", description: "Initial pane/tab configuration rendered on mount." },
    renderTabContent: { type: "(paneId: string, tabId: string) => ReactNode", required: true, description: "Renders content for the active tab in each pane. Called on every render — keep it fast." },
    onBeforeCloseTab: { type: "(paneId: string, tabId: string, tab: WorkspaceTabDef) => boolean | Promise<boolean>", description: "Return false to veto a tab close — use it to prompt on unsaved changes. Consulted for every tab a closePane would discard; one veto aborts the whole pane close." },
    fallback: { type: "ReactNode", description: "Shown when all panes are closed. Defaults to a built-in placeholder." },
    className: { type: "string", description: "Additional CSS classes applied to the root element." },
  }}
/>

### WorkspacePaneDef

Used in `initialPanes` and `openPane()`.

<TypeTable
  type={{
    id: { type: "string", required: true, description: "Unique identifier for this pane." },
    tabs: { type: "WorkspaceTabDef[]", required: true, description: "Initial tabs for this pane." },
    defaultActiveTabId: { type: "string", description: "Which tab is active on mount. Defaults to the first tab." },
    defaultSize: { type: "number", description: "Percentage width (0–100) for horizontal panel groups." },
    minSize: { type: "number", description: "Minimum percentage size. Defaults to 20." },
    onAddTab: { type: "() => void", description: 'Renders a "+" button in the tab strip when provided.' },
  }}
/>

### WorkspaceTabDef

A single tab. Re-exported alias of `WorkspaceTab` from `workspace-tabs`.

<TypeTable
  type={{
    id: { type: "string", required: true, description: "Unique identifier for this tab." },
    title: { type: "string", required: true, description: "Label shown in the tab strip." },
    icon: { type: "ReactNode", description: "Rendered before the title, sized to 14px." },
    badge: { type: "number", description: "Unread count. Values above 99 render as “99+”; 0 and undefined render nothing." },
    pinned: { type: "boolean", default: "false", description: "Pinned tabs cannot be closed or dragged, and render no close button." },
    dirty: { type: "boolean", default: "false", description: "Unsaved changes: the tab shows a dot instead of its close button until hovered. Pair with onBeforeCloseTab to prompt before discarding." },
  }}
/>

### useWorkspace()

Context hook available to any component rendered inside `<Workspace>`.

<TypeTable
  type={{
    panes: { type: "readonly PaneState[]", description: "Current flat list of all open panes." },
    isShowingFallback: { type: "boolean", description: "True when all panes are closed and the fallback is visible." },
    lastActivePaneId: { type: "string | null", description: "ID of the most recently focused pane." },
    openTabInPane: { type: "(paneId, tab) => void", description: "Opens or focuses a tab in the specified pane." },
    closeTab: { type: "(paneId, tabId) => void", description: "Closes a tab, subject to onBeforeCloseTab. Removes the pane if it was the last tab." },
    activateTab: { type: "(paneId, tabId) => void", description: "Switches the active tab in a pane." },
    updateTab: { type: "(paneId, tabId, patch) => void", description: "Patches tab properties (e.g. update badge count)." },
    openPane: { type: "(pane: WorkspacePaneDef) => void", description: "Adds a new pane column. No-op if the pane ID already exists." },
    closePane: { type: "(paneId) => void", description: "Removes a pane and all its tabs, subject to onBeforeCloseTab." },
  }}
/>

### WorkspaceHandle

Imperative ref API. Attach via `ref` on `<Workspace>`. Same methods as `useWorkspace()`, minus read-only pane state and plus layout persistence.

<TypeTable
  type={{
    lastActivePaneId: { type: "string | null", description: "ID of the most recently focused pane." },
    openTabInPane: { type: "(paneId, tab) => void", description: "Opens or focuses a tab in the specified pane." },
    closeTab: { type: "(paneId, tabId) => void", description: "Closes a tab, subject to onBeforeCloseTab. Removes the pane if it was the last tab." },
    activateTab: { type: "(paneId, tabId) => void", description: "Switches the active tab in a pane." },
    updateTab: { type: "(paneId, tabId, patch) => void", description: "Patches tab properties (e.g. update badge count)." },
    openPane: { type: "(pane: WorkspacePaneDef) => void", description: "Adds a new pane column. No-op if the pane ID already exists." },
    closePane: { type: "(paneId) => void", description: "Removes a pane and all its tabs, subject to onBeforeCloseTab." },
    serialize: { type: "() => WorkspaceSnapshot", description: "Captures the current column/pane/tab structure for persistence." },
    restore: { type: "(snapshot, resolveTab) => void", description: "Rebuilds the layout from a snapshot. resolveTab maps a tab id back to a full tab." },
  }}
/>

### WorkspaceSnapshot

A JSON-safe view of the layout: column, pane, and tab **ids** only. A tab's
`icon` is a `ReactNode` and a pane's `onAddTab` a function, so neither survives
a round-trip through storage — `restore` takes a `resolveTab` callback to
rehydrate each tab from its id. Tabs that don't resolve are dropped, along with
any pane they'd leave empty. Panel *sizes* aren't part of the snapshot.

```tsx
const workspace = React.useRef<WorkspaceHandle>(null)

React.useEffect(() => {
  const saved = localStorage.getItem("workspace")
  if (saved) workspace.current?.restore(JSON.parse(saved), (id) => TABS[id])

  const save = () =>
    localStorage.setItem("workspace", JSON.stringify(workspace.current?.serialize()))
  window.addEventListener("beforeunload", save)
  return () => window.removeEventListener("beforeunload", save)
}, [])
```
