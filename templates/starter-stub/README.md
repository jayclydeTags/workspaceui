# Starter Stub

Placeholder template for the WorkspaceUI Templates catalog — the walking
skeleton that proves the catalog end to end (browse → detail → download → run).

```bash
pnpm install
pnpm dev
```
